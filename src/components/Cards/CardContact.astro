---

import { Icon } from 'astro-icon/components'
import '@fontsource-variable/oswald';
import '@fontsource/roboto';

interface Props {
  bgColor: string;
  icon: string;
  title: string;
  text: string;
  text02: string;
}

const { bgColor, icon, title, text, text02 } = Astro.props;
---

<!--pasamos como props el icons el color de fondo, el titulo, el text, el link y el color del boton -->
<div class={`flex flex-col justify-evenly  gap-4 w-72 h-96 md:w-72 md:h-[350px] px-10 py-10  shadow-xl rounded-xl ${bgColor}`}>
    <Icon name={icon} class="w-8 h-8 scale-110 " />
    <h3 class="text-xl font-bold w-32 text-white font-Oswald" >{title}</h3>
    <p class="font-roboto text-white">{text}</p>
    <p class={ `font-Oswald  w-full h-14 p-5 flex justify-center items-center rounded-2xl bg-white text-blue-600 font-bold text-center text-base shadow-sm`}>{text02}</p>

  </div>
